// Deckname: darf NIE als Einstieg gelesen werden - der Zaun soll das ECHTE
// Blatt lesen, nicht diesen Text, der nur als `src=…` in einem fremden
// Attributwert steht (Kritik #182, dritte Form).
console.log('decoy')
