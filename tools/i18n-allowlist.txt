# i18n hard-gate allowlist
#
# One entry per line.
# - Exact literal match: copied as-is
# - Regex match: prefix with `re:`
#
# Keep this list tight. Every entry should be reviewable and justified.

MB
kWh
kW
°C
URL
HTTP
HTTPS
API
JSON
CSV
UUID
MQTT
KNX
# Dynamic :title bindings — computed expressions, not string literals
re:^nodeFullPath\(
re:^ownerTitle\(
re:^f\.label
# Technical placeholders / examples in NodeConfigPanel (formulas, JSON examples, external URLs)
a + b
crontab.guru ↗
re:^x \* 100$
re:^\{"X-Api-Key"

# GenericNode.vue — NODE_DEFS static node-type labels (first label: per entry).
# Port labels are now fully translated via t(); the node-type labels below are
# either single-word technical tokens (auto-passed) or require an allowlist entry.
re:^(Max|Min)∞$
re:^(JSON|XML) Extraktor$
re:^SMS \(seven\.io\)$
re:^∅
Verzögerung
MCP Tool
Astro Sonne
Tagsüber
Temp °C
Zähler
String Verketten
String Suchen/Ersetzen
API Client
Substring / RegEx
Wake on LAN

# SUPPORTED_LOCALES native language names (i18n.js / i18n.ts).
# These are intentionally hardcoded: they must appear in their own script before
# any locale is loaded (bootstrapping), so $t() cannot be used here.
# ASCII-only names (Deutsch, English, Italiano, Schweizerdeutsch) pass the guard
# automatically via is_technical_token; only names with non-ASCII characters need
# an explicit entry.
Español
Français

# SchemaForm.vue — defaultPlaceholder() returns a schema default value (e.g. "502", "3.0")
# from the backend JSON schema, not a hardcoded user-facing string.
re:^defaultPlaceholder\(

# visuWidgetTypes.js — Programmierfehler-Code, kein Text fuer Nutzer. Ein
# Widget-Typ ausserhalb der neun Kern-Typen ist ein Aufruffehler im Editor-Code
# (Goldene Regel 3: laut scheitern statt still ersetzen); die Meldung ist ein
# maschinenlesbarer Code im Haus-Stil von `clipboard_copy_failed` und erreicht
# keine Oberflaeche.
re:^unknown_core_widget_type
